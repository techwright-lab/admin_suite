<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Developer Portal - Sign In" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Gleania Developer">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="<%= asset_path('logo/logo.png') %>" type="image/png">
    <link rel="apple-touch-icon" href="<%= asset_path('logo/logo.png') %>">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-slate-50 dark:bg-slate-900">
    <div class="min-h-screen flex flex-col justify-center py-12 sm:px-6 lg:px-8">
      <!-- Flash messages -->
      <div class="sm:mx-auto sm:w-full sm:max-w-md">
        <%= render "shared/flash" %>
      </div>

      <%= yield %>
    </div>
  </body>
</html>
