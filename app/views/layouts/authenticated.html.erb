<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Gleania" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Gleania">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="<%= asset_url('logo/logo.png') %>" type="image/png">
    <link rel="apple-touch-icon" href="<%= asset_url('logo/logo.png') %>">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 dark:bg-gray-900" data-controller="ai-assistant">
    <div class="flex h-screen overflow-hidden">
      
      <!-- Sidebar -->
      <div class="hidden lg:flex lg:flex-shrink-0">
        <%= render "shared/sidebar" %>
      </div>

      <!-- Mobile sidebar overlay -->
      <div data-sidebar-target="overlay" 
           class="hidden fixed inset-0 z-40 bg-gray-600 bg-opacity-75 lg:hidden"
           data-action="click->sidebar#close">
      </div>

      <!-- Mobile sidebar -->
      <div data-sidebar-target="mobileSidebar"
           class="hidden fixed inset-y-0 left-0 z-50 w-64 transform transition-transform duration-300 ease-in-out lg:hidden">
        <%= render "shared/sidebar" %>
      </div>

      <!-- Main content area (shrinks when assistant is open) -->
      <div class="flex flex-col flex-1 min-w-0 overflow-hidden transition-all duration-300" data-ai-assistant-target="mainContent">
        
        <!-- Top bar -->
        <%= render "shared/topbar" %>

        <!-- Main content -->
        <main class="flex-1 overflow-hidden bg-gray-50 dark:bg-gray-900">
          
          <!-- Flash messages -->
          <%= render "shared/flash" %>

          <!-- Page content -->
          <% if content_for?(:full_bleed) %>
            <%= yield %>
          <% else %>
            <div class="py-6 h-full overflow-y-auto">
              <%= yield %>
            </div>
          <% end %>
        </main>
      </div>

      <!-- AI Assistant Panel (integrated side panel) -->
      <%= render "shared/ai_assistant_panel" %>
    </div>

    <!-- Floating button (visible when panel is closed) -->
    <button
      data-action="click->ai-assistant#toggle"
      data-ai-assistant-target="button"
      class="fixed bottom-24 right-6 px-4 py-3 gradient-brand rounded-full shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-200 flex items-center gap-2 z-40"
      title="Ask Gleania (Cmd+J)"
    >
      <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"/>
      </svg>
      <span class="text-white text-sm font-medium">Ask Gleania</span>
    </button>
  </body>
</html>
