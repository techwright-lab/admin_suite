<% content_for :title, "Edit #{resource_config&.human_name || resource_name.singularize.humanize}" %>

<div class="max-w-3xl">
  <h1 class="text-2xl font-bold text-slate-900 dark:text-white mb-4">
    Edit <%= resource_config&.human_name || resource_name.singularize.humanize %>
  </h1>

  <div class="bg-white dark:bg-slate-800 rounded-xl border border-slate-200 dark:border-slate-700 p-6">
    <%= render "form", resource: @resource %>
  </div>
</div>
