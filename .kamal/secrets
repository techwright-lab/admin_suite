# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Example of extracting secrets from 1password (or another compatible pw manager)
# SECRETS=$(kamal secrets fetch --adapter 1password --account your-account --from Vault/Item KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})

# Example of extracting secrets from Rails credentials
KAMAL_REGISTRY_PASSWORD=$(rails credentials:fetch --environment production kamal.registry_password)
POSTGRES_PASSWORD=$(rails credentials:fetch --environment production postgres_password)
LOGTAIL_TOKEN=$(rails credentials:fetch --environment production logtail.token)
LOGTAIL_INGESTING_HOST=$(rails credentials:fetch --environment production logtail.ingesting_host)
POSTMARK_API_TOKEN=$(rails credentials:fetch --environment production postmark.api_token)
OPENAI_API_KEY=$(rails credentials:fetch --environment production openai.api_key)
ANTHROPIC_API_KEY=$(rails credentials:fetch --environment production anthropic.api_key)
# GEMINI_API_KEY=$(rails credentials:fetch gemini.api_key)
# GREENHOUSE_API_TOKEN=$(rails credentials:fetch greenhouse.api_token)
# LEVER_API_TOKEN=$(rails credentials:fetch lever.api_token)
GOOGLE_CLIENT_ID=$(rails credentials:fetch --environment production google.client_id)
GOOGLE_CLIENT_SECRET=$(rails credentials:fetch --environment production google.client_secret)
CLOUDFLARE_TURNSTILE_SITE_KEY=$(rails credentials:fetch --environment production cloudflare.turnstile_site_key)
CLOUDFLARE_TURNSTILE_SECRET_KEY=$(rails credentials:fetch --environment production cloudflare.turnstile_secret_key)
CLOUDFLARE_ACCOUNT_ID=$(rails credentials:fetch --environment production cloudflare.account_id)
CLOUDFLARE_ACCESS_KEY_ID=$(rails credentials:fetch --environment production cloudflare.access_key_id)
CLOUDFLARE_ACCESS_KEY_SECRET=$(rails credentials:fetch --environment production cloudflare.access_key_secret)
CLOUDFLARE_BUCKET=$(rails credentials:fetch --environment production cloudflare.bucket)
CLOUDFLARE_API_TOKEN=$(rails credentials:fetch --environment production cloudflare.api_token)

# Use a GITHUB_TOKEN if private repositories are needed for the image
# GITHUB_TOKEN=$(gh config get -h github.com oauth_token)

# Grab the registry password from ENV
# KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Improve security by using a password manager. Never check config/master.key into git!
RAILS_MASTER_KEY=$(cat config/credentials/production.key)
