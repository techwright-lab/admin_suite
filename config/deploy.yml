# Name of your application. Used to uniquely configure containers.
service: gleania

# Name of the container image (use your-user/app-name on external registries).
image: techwright-lab/gleania

# Deploy to these servers.
servers:
  web:
    - 138.199.230.71
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# If used with Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
#
# Using an SSL proxy like this requires turning on config.assume_ssl and config.force_ssl in production.rb!
#
# Don't use this when deploying to multiple web servers (then you have to terminate SSL at your load balancer).
#
proxy:
  ssl: true
  host: gleania.com
  forward_headers: true

# Where you keep your container images.
registry:
  # Specify the registry server, if you're not using Docker Hub
  server: ghcr.io
  username: techwright-lab

  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - POSTGRES_PASSWORD
    - LOGTAIL_TOKEN
    - LOGTAIL_INGESTING_HOST
    - POSTMARK_API_TOKEN
    - OPENAI_API_KEY
    - ANTHROPIC_API_KEY
    - GOOGLE_CLIENT_ID
    - GOOGLE_CLIENT_SECRET
    - CLOUDFLARE_TURNSTILE_SITE_KEY
    - CLOUDFLARE_TURNSTILE_SECRET_KEY
    - CLOUDFLARE_ACCOUNT_ID
    - CLOUDFLARE_ACCESS_KEY_ID
    - CLOUDFLARE_ACCESS_KEY_SECRET
    - CLOUDFLARE_BUCKET
    - CLOUDFLARE_API_TOKEN
    - CLOUDFLARE_PUBLIC_URL
    - CLOUDFLARE_ASSETS_URL
    - LEMONSQUEEZY_API_KEY
    - LEMONSQUEEZY_STORE_ID
    - LEMONSQUEEZY_WEBHOOK_SECRET
    - OTEL_EXPORTER_OTLP_HEADERS
  clear:
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    RAILS_LOG_LEVEL: info
    RAILS_ENV: production
    AWS_REGION: "eu-central-1"
    BASE_URL: "https://gleania.com"
    RAILS_SERVE_STATIC_FILES: true
    # PostgreSQL database configuration
    DB_HOST: 10.0.0.2  # Replace with your PostgreSQL server IP
    DB_PORT: 5432
    DB_USERNAME: twl_db_admin
    POSTGRES_DB: gleania_production
    OPENAI_ORGANIZATION_ID: org-pjb9CpfGrDZbkQf5s0gvxwu2
    APP_DOMAIN: gleania.com
    OTEL_EXPORTER_OTLP_ENDPOINT: https://otlp-gateway-prod-eu-west-2.grafana.net/otlp/
    OTEL_TRACES_EXPORTER: otlp
    OTEL_LOGS_EXPORTER: otlp
    OTEL_EXPORTER_OTLP_PROTOCOL: http/protobuf
# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "gleania_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets


# Configure the image builder.
builder:
  arch: amd64
  dockerfile: Dockerfile
  args:
    RAILS_ENV: <%= ENV["RAILS_ENV"] || "production" %>
    RUBY_VERSION: "3.4.7"

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-3.4.7
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# accessories:
#   db:
#     image: postgres:16
#     host: 188.245.112.83  # Replace with your PostgreSQL server IP
#     # Expose port 5432 to the world (remove 127.0.0.1: prefix for external access)
#     port: "5432:5432"
#     env:
#       clear:
#         POSTGRES_DB: gleania_production
#         POSTGRES_USER: twl_db_admin
#         POSTGRES_MULTIPLE_DATABASES: gleania_production_cache,gleania_production_queue,gleania_production_cable
#       secret:
#         - POSTGRES_PASSWORD
#     files:
#       - config/postgres/init-multiple-databases.sh:/docker-entrypoint-initdb.d/init-multiple-databases.sh
#     directories:
#       - data:/var/lib/postgresql/data